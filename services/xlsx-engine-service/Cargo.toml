[package]
name = "xlsx-engine-service"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
axum = "0.8"
base64 = "0.22"
ironcalc = "0.7.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "signal", "sync"] }
uuid = { version = "1", features = ["v4", "serde"] }
xlsx-sidecar = { path = "../../apps/sheets/native/xlsx-engine" }
